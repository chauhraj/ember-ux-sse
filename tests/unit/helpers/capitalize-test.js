
import { capitalize } from 'ember-ux-sse/helpers/capitalize';
import { module, test } from 'qunit';

module('Unit | Helper | capitalize');

// Replace this with your real tests.
test('it should capitalize the passed words', function(assert) {
  let result = capitalize([42]);
  assert.ok(result);
});
